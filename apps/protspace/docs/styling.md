# Annotation Styling

This page has moved to the ProtSpace documentation site:

**https://protspace.app/docs/guide/styling**

It covers the `protspace style` workflow, the styles JSON format, color palettes, legend ordering,
and how CLI styling keys are reinterpreted for numeric annotations.
