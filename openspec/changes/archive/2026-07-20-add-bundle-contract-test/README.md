# add-bundle-contract-test

Add a cross-language contract test that generates `.parquetbundle` files with the real `protspace bundle` CLI and reads them back with the real web reader, wired to a CI job whose path filter covers both sides.
