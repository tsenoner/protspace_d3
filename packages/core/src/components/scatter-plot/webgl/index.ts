// WebGL Renderer Exports
export { WebGLRenderer } from './renderer/webgl-renderer';

// Constants
export { MAX_RENDERABLE_POINTS } from './types';

// Shared export-sizing formula (dots + badge capture, #302)
export { computeSizeScaleFactor } from './renderer/viewport-defaults';
